# Plan 0.1 — Repo scaffold

Task: `docs/12-implementation-plan.md` M0 / 0.1. Depends: —.
Traces to: NFR "CI must be green" (`docs/14-engineering-standards.md` § Git and review).
No `US-xx`/`T-xx`: this is foundation, it enables T-17 in 0.2.

## Steps

| # | Change | Files | Verify |
|---|--------|-------|--------|
| 1 | `git init`, branch `main`, commit existing docs as baseline | — | `git log --oneline` |
| 2 | Ignore rules for Go, Python, node, generated artefacts, env | `.gitignore` | `git status` clean after a build |
| 3 | Editor defaults: LF, utf-8, 4-space Go/tab, 4-space Python, 2-space yaml | `.editorconfig` | visual |
| 4 | Ownership | `CODEOWNERS` | — |
| 5 | Makefile with every target named in `CLAUDE.md` § Commands; unimplemented ones fail loudly instead of silently passing | `Makefile` | `make lint` exits 0 on empty repo |
| 6 | CI skeleton: lint-only job (editorconfig + gitleaks + `make lint`) on push/PR | `.github/workflows/ci.yml` | workflow parses |

## Out of scope

Go/Python toolchains, proto, compose — those are 0.2/0.3/0.6. `make test` etc. are
declared but no-op until their task lands; each prints what it is waiting on.
